## KernelGuard Blue Submission

KernelGuard-Red-Submission: 0

Replace `0` with the exact red submission ID this PR is meant to fix.

## Notes

- This PR will be registered against the private KernelGuard competition control plane from a trusted `pull_request_target` workflow that only reads PR metadata.
- Hidden-suite evaluation and result posting happen through the private competition control plane, not from public PR workflow code in this repo.
